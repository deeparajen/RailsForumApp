<%= simple_form_for([post, post.comments.build],remote: true) do |f| %>
<!--
collection.build(attributes = {}, …) Returns one or more new objects of the collection type that have been instantiated with attributes and linked to this object through a foreign key, but have not yet been saved. Note: This only works if an associated object already exists, not if it‘s nil!
-->
<div class="form-group">
	<%= f.text_area :comment, hide_label: true, placeholder: "Leave a comment...", cols: 60,:id => "comment_#{post.id}"%>
</div>
            <div class="form-group">
            	<%= f.button :submit, 'Leave a reply', class: "button is-primary" %>
               
            </div>

<% end %>