
  <%= form_for(@post) do |f| %>
    <div class="form-group">
    <%= f.label :content %>
    <%= f.text_area :content, class: 'form-control', required: true, cols: 3 %>
   </div>
    <div class="form-group">
      
      <%= f.submit 'Post', class: 'btn btn-primary'%>
    </div>
  <% end %>
